# Jarvis algorithm in asm

This is an implementation of the jarvis algorithm in asm

it displays the point cloud in a X window

## Prerequisits

- NASM compiler
- libX11
  
## Running

To run the program:
```bash
make
```

## Cleaning up

To clean up the .o and the executable file written by make:
```bash
make clean
```